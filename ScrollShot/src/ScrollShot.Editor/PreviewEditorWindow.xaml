<Window x:Class="ScrollShot.Editor.PreviewEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ScrollShot.Editor.Controls"
        mc:Ignorable="d"
        Title="ScrollShot Preview"
        Height="860"
        Width="1280">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="0" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="220" />
        </Grid.ColumnDefinitions>

        <ToolBar Grid.Row="0"
                 Grid.ColumnSpan="2">
            <Button Content="Undo"
                    Command="{Binding UndoCommand}" />
            <Button Content="Redo"
                    Command="{Binding RedoCommand}" />
            <Separator />
            <Button Content="Chrome"
                    Command="{Binding ToggleChromeCommand}" />
            <Button Content="Save"
                    Command="{Binding SaveCommand}" />
            <Button Content="Copy"
                    Command="{Binding CopyCommand}" />
            <Button Content="Discard"
                    Command="{Binding DiscardCommand}" />
        </ToolBar>

        <controls:ImageViewport x:Name="ViewportControl"
                                Grid.Row="1"
                                Grid.Column="0"
                                Margin="8" />

        <controls:TimelineStrip x:Name="TimelineStripControl"
                                Grid.Row="1"
                                Grid.Column="1"
                                Margin="0,8,8,8" />
    </Grid>
</Window>
